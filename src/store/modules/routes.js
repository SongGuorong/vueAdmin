


// state
const state = () => ({
    routes: [],
    partialRoutes: [],
});

// getters
const getters = {
    routes: (state) => state.routes,
    partialRoutes: (state) => state.partialRoutes,
};

// mutations
const mutations = {
    setRoutes(state, routes) {
    }
}


// actions


